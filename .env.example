DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=manufacturing_analytics
DB_PORT=3306

# Only needed for hosted MySQL providers that require SSL (e.g. Aiven).
# Download the CA cert from your provider and set the path here.
# Leave blank for local MySQL.
DB_SSL_CA=
